import java.util.Scanner;

public class ReportCard {
    private String studentName;
    private int studentClass;
    private int studentID;
    private int mathGrade;
    private int scienceGrade;
    private int englishGrade;
    private int computerSciGrade;
    private int spanishGrade;

    // Constructor
    public ReportCard(String studentName, int studentClass, int mathGrade, int scienceGrade, int englishGrade, int computerSciGrade, int spanishGrade) {
        this.studentName = studentName;
        this.studentClass = studentClass;
        this.mathGrade = mathGrade;
        this.scienceGrade = scienceGrade;
        this.englishGrade = englishGrade;
        this.computerSciGrade = computerSciGrade;
        this.spanishGrade = spanishGrade;
    }

    // Method to calculate the average grade
    public double calculateAverageGrade() {
        double gradeAvg = (mathGrade + scienceGrade + englishGrade + computerSciGrade + spanishGrade) / 5.0;
            return gradeAvg;
    }

    // Method to display the report card
    public void displayReportCard() {
        System.out.println("\n\nREPORT CARD POSTED:");
        System.out.println("Student Name: " + studentName);
        System.out.println("Student Class: " + studentClass);
        System.out.println("Math Grade: " + mathGrade);
        System.out.println("Science Grade: " + scienceGrade);
        System.out.println("English Grade: " + englishGrade);
        System.out.println("Comp Sci Grade: " + computerSciGrade);
        System.out.println("Spanish Grade: " + spanishGrade);
        System.out.println("Average Grade: " + calculateAverageGrade());
    
        double averageGrade = calculateAverageGrade();
    
        // Determine letter grade based on average grade
        String letterGrade;
        if (averageGrade > 95.0) {
            letterGrade = "A+";
        } else if (averageGrade > 92.5) {
            letterGrade = "A";
        } else if (averageGrade > 89.5) {
            letterGrade = "A-";
        } else if (averageGrade > 86.5) {
            letterGrade = "B+";
        } else if (averageGrade >82.5){
            letterGrade = "B";
        } else if (averageGrade > 79.5){
            letterGrade = "B-";
        } else if (averageGrade > 76.5) {
            letterGrade = "C+";
        } else if (averageGrade >72.5){
            letterGrade = "C";
        } else if (averageGrade > 69.5){
            letterGrade = "C-";
        } else if (averageGrade > 66.5) {
            letterGrade = "D+";
        } else if (averageGrade >62.5){
            letterGrade = "D";
        } else if (averageGrade > 59.5){
            letterGrade = "D-";
        } else{
            letterGrade = "F";
        }
        System.out.println("Letter Grade: " + letterGrade);

    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Take user input for student details
        System.out.println("Enter Student Name: ");
        String studentName = scanner.nextLine();
        
        System.out.println("Enter Student Class:");
        int studentClass = scanner.nextInt();

        System.out.println("Enter Math Grade: ");
        int mathGrade = scanner.nextInt();

        System.out.println("Enter Science Grade: ");
        int scienceGrade = scanner.nextInt();

        System.out.println("Enter English Grade: ");
        int englishGrade = scanner.nextInt();

        System.out.println("Enter Computer Sci Grade: ");
        int computerSciGrade = scanner.nextInt();

        System.out.println("Enter Spanish Grade: ");
        int spanishGrade = scanner.nextInt();

        // Create a ReportCard object with user input
        ReportCard studentReport = new ReportCard(studentName,studentClass, mathGrade, scienceGrade, englishGrade, computerSciGrade, spanishGrade);

        // Display the report card
        studentReport.displayReportCard();

        scanner.close();
    }
}
